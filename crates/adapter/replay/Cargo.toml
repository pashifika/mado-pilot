[package]
name = "mado-pilot-adapter-replay"
description = "MadoPilot deterministic replay capture adapter for file and memory sources."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
mado-pilot-core = { path = "../../automation/core" }
mado-pilot-capture = { path = "../../automation/capture" }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
mado-pilot-testkit = { path = "../../support/testkit" }

[lints]
workspace = true
