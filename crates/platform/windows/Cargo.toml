[package]
name = "mado-pilot-platform-windows"
description = "MadoPilot native Windows discovery, capture, and input adapter."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[features]
benchmark-instrumentation = []

[target.'cfg(windows)'.dependencies]
mado-pilot-core = { path = "../../automation/core" }
mado-pilot-capture = { path = "../../automation/capture" }
mado-pilot-input = { path = "../../automation/input" }
windows.workspace = true

[lints]
workspace = true
