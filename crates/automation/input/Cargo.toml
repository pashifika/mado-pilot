[package]
name = "mado-pilot-input"
description = "MadoPilot input operation, delivery-mode, focus, receipt, and error contracts."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
mado-pilot-core = { path = "../core" }

[lints]
workspace = true
