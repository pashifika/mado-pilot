[package]
name = "mado-pilot-vision"
description = "MadoPilot template source, preprocessing, matching request, and result contracts."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
mado-pilot-core = { path = "../core" }

[lints]
workspace = true
