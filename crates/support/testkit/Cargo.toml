[package]
name = "mado-pilot-testkit"
description = "MadoPilot controlled capture and backend doubles, synthetic clock, and contract-suite support."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
mado-pilot-core = { path = "../../automation/core" }
mado-pilot-capture = { path = "../../automation/capture" }
mado-pilot-input = { path = "../../automation/input" }
mado-pilot-ocr = { path = "../../automation/ocr" }
mado-pilot-vision = { path = "../../automation/vision" }

# Verifying that a tracked fixture directory still matches its SHA256SUMS.
sha2 = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
