# Phase 2.2 macOS process-directed diagnostics, measured on aarch64-apple-darwin at source
# b1059cf6239042107bd62373eb65211117beaab9 and tree
# 0fffe5587d29bed8c6908e7b852617be8b381c13.
#
# Produced by `crates/mado-pilot/benches/native-phase2.rs`. ADR 0029
# accepts this revision-bound run and its pre-measurement qualification
# budgets. These are regression ceilings, not user-facing promises.

format_version = 1

[benchmark]
id = "phase-2-2-process-directed-diagnostics-aarch64-apple-darwin"
workload = "macOS process-directed posting with diagnostics Off, Normal, Debug, and overflow"
phase = "2.2"
status = "measured"
normative = true
measurements_recorded = true

[profile]
fixture = "separately linked private macOS fixture Rust, protocol-v5, header, Objective-C, and renderer sources"
fixture_sha256 = "2252287e99fc2f14645326eb76b8c3b4a9ba4bb53f81fdf196f172d9ee3ac851"
release_target = "aarch64-apple-darwin"
hardware = "Apple M1 Pro, 10 cores, 32 GiB"
os_version = "macOS 26.5.2 (25F84)"
build_profile = "cargo bench, default features, debug_assertions=false; fixture cargo build --release --features private-fixture; signed .app launch"
warmup_iterations = 20
sample_count = 200
correctness_oracle = "every retained sample checks complete frame identity/content, transition state, invocation-only receipt, separate fixture event, diagnostics, or common-flow outcome as its measurement states; a private acknowledgement never substitutes for product delivery or visual progress"
queue_policy = "diagnostics Off has no queue; Normal and Debug use capacity 64; overflow uses capacity 4; every sample drains"
notes = "source commit b1059cf6239042107bd62373eb65211117beaab9, tree 0fffe5587d29bed8c6908e7b852617be8b381c13; toolchain rustc 1.97.1; Apple clang 21.0.0; macOS SDK 26.5; GPU/driver Apple integrated GPU; system driver stack; display topology three online non-mirrored displays; public frame geometry: 3840x2160 logical/backing 1x at (-3840,-829), 2560x1440 logical/5120x2880 backing 2x main at (0,0), 1512x982 logical/3024x1964 backing 2x at (2560,190); mixed 2x/1x signed-origin seam and same-scale 2x/2x seam; permissions/signing Screen Recording granted; event-post access granted; generated fixture bundle ad-hoc signed with approved identifier; fixture executable sha256 1360206fc656a55385bde2c7d5f36badc869a8966731786ebd5f0b80ff47e7a2; fixture path deliberately omitted; lineage=process-directed-diagnostics; mode=default renderer=appkit-background"

[[budget]]
measure = "result_correctness"
kind = "hard"
requirement = "every retained sample satisfies its workload's oracle"
predicate = "result_correctness == 0"

[[budget]]
measure = "allocated_growth_bytes"
kind = "hard"
requirement = "a repeated operation gives back what it took"
predicate = "allocated_growth_bytes <= 4096"

[[budget]]
measure = "peak_allocated_bytes"
kind = "absolute"
unit = "bytes"
direction = "at_most"
limit = 16777216
rationale = "the frozen 16 MiB process-directed live Rust heap ceiling"

[[measurement]]
workload = "event_diagnostics_off"
correctness_oracle = "the process-directed invocation-only receipt and separate pointer observation are exact and no diagnostic reader exists"
result_correctness = 0
latency_p50_ms = 159.394459000
latency_p95_ms = 165.798083000
latency_max_ms = 170.064417000
iteration_span_ms = 161.242073000
mapped_bytes_per_result = 0
peak_allocated_bytes = 4674650
steady_allocated_bytes = 4673314
allocated_growth_bytes = 0

[[measurement.budget]]
  measure = "latency_p50"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 300.0
  rationale = "frozen before implementation by the Phase 2.2 qualification plan"

  [[measurement.budget]]
  measure = "latency_p95"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 750.0
  rationale = "frozen before implementation by the Phase 2.2 qualification plan"

  [[measurement.budget]]
  measure = "latency_max"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 2000.0
  rationale = "frozen hard scenario bound from the Phase 2.2 qualification plan"

[[measurement]]
workload = "event_diagnostics_normal"
correctness_oracle = "the same process-directed receipt and observation hold while exactly one normal terminal record drains without loss"
result_correctness = 0
latency_p50_ms = 159.622375000
latency_p95_ms = 165.047625000
latency_max_ms = 168.632750000
iteration_span_ms = 161.123376000
mapped_bytes_per_result = 0
peak_allocated_bytes = 4684818
steady_allocated_bytes = 4683786
allocated_growth_bytes = 0

[[measurement.budget]]
  measure = "latency_p50"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 300.0
  rationale = "frozen before implementation by the Phase 2.2 qualification plan"

  [[measurement.budget]]
  measure = "latency_p95"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 750.0
  rationale = "frozen before implementation by the Phase 2.2 qualification plan"

  [[measurement.budget]]
  measure = "latency_max"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 2000.0
  rationale = "frozen hard scenario bound from the Phase 2.2 qualification plan"

[[measurement]]
workload = "event_diagnostics_debug"
correctness_oracle = "the same process-directed receipt and observation hold while debug start, route attempt, per-event observation, and normal terminal records drain in order without loss"
result_correctness = 0
latency_p50_ms = 160.103625000
latency_p95_ms = 165.193625000
latency_max_ms = 169.715458000
iteration_span_ms = 161.743952000
mapped_bytes_per_result = 0
peak_allocated_bytes = 4685154
steady_allocated_bytes = 4683850
allocated_growth_bytes = 0

[[measurement.budget]]
  measure = "latency_p50"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 300.0
  rationale = "frozen before implementation by the Phase 2.2 qualification plan"

  [[measurement.budget]]
  measure = "latency_p95"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 750.0
  rationale = "frozen before implementation by the Phase 2.2 qualification plan"

  [[measurement.budget]]
  measure = "latency_max"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 2000.0
  rationale = "frozen hard scenario bound from the Phase 2.2 qualification plan"

[[measurement]]
workload = "event_diagnostic_overflow"
correctness_oracle = "four complete process-directed receipts and separate pointer observations remain exact while the four-slot queue retains normal records and reports every debug loss"
result_correctness = 0
latency_p50_ms = 162.511791000
latency_p95_ms = 167.470750000
latency_max_ms = 169.508500000
iteration_span_ms = 640.563605000
mapped_bytes_per_result = 0
peak_allocated_bytes = 4677674
steady_allocated_bytes = 4674250
allocated_growth_bytes = 0

[[measurement.budget]]
  measure = "latency_p50"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 300.0
  rationale = "frozen before implementation by the Phase 2.2 qualification plan"

  [[measurement.budget]]
  measure = "latency_p95"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 750.0
  rationale = "frozen before implementation by the Phase 2.2 qualification plan"

  [[measurement.budget]]
  measure = "latency_max"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 2000.0
  rationale = "frozen hard scenario bound from the Phase 2.2 qualification plan"
