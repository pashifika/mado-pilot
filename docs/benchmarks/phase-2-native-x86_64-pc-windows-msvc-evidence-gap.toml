# Phase 2 native Windows performance evidence gap.
#
# The accepted plan requires the same capture, transition, input, C, and C++
# workload families on a supported bare-metal Windows host. No such host was
# available for this revision. This file records that absence instead of copying
# macOS values, CI cross-build timings, or the earlier G-002 ownership prototype
# into a Windows performance claim.

format_version = 1

[benchmark]
id = "phase-2-native-evidence-gap-x86_64-pc-windows-msvc"
workload = "native capture, transitions, input, and public-language flows not measured"
phase = "2"
status = "evidence-gap"
normative = false
measurements_recorded = false

[profile]
fixture = "crates/platform/windows fixture Rust and protocol sources"
fixture_sha256 = "37a1d135a62cb3effba9276ac5546269f0e8b0372d840a2e173be0c9b168a278"
release_target = "x86_64-pc-windows-msvc"
hardware = "not measured; approved bare-metal Windows host unavailable"
os_version = "not measured; candidate support floor is recorded by G-001"
build_profile = "not measured; required command is cargo bench --locked --package mado-pilot --bench native-phase2"
warmup_iterations = 0
sample_count = 0
correctness_oracle = "not exercised; each workload's oracle is implemented by native-phase2 and must pass before a Windows profile is accepted"
queue_policy = "session latest-wins queue depth 1; adapter process-shared retained-storage ceilings"
notes = "source commit a1faf04505c8471deb4de8c136fddcc7f76105e7, tree a6a3edd6e627eadc9da76785c861136d669e8b05; no hosted CI timing is substituted for bare-metal capture, display, input, GPU/driver, permission, or signing evidence; run all three workload sets on the same reviewed revision and replace this gap with three measured profiles"

[measurements]
performed = false
reason = "a supported bare-metal Windows host with an interactive desktop was unavailable"
