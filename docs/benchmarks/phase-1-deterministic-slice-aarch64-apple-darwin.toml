# Phase 1 deterministic slice, measured on aarch64-apple-darwin.
#
# Produced by `crates/mado-pilot/benches/deterministic-slice.rs`:
#
#   cargo bench --locked --package mado-pilot --bench deterministic-slice -- \
#       --hardware "Apple M1 Pro, 10 cores, 32 GiB" \
#       --os-version "macOS 26.5.2 build 25F84"
#
# Every measurement below is that run's output, unedited. The budgets are added
# here and are set by `docs/adr/0008-phase-1-performance-budgets.md`, which
# resolved the Phase 1 workloads under `G-013`.
#
# These budgets are regression ceilings, not product requirements, and they are
# valid only for this target. Each was set at three times the value ADR 0008
# measured, rounded up to a readable number: loose enough to survive a loaded
# developer machine, tight enough that a structural regression fails. A ceiling
# keeps the number ADR 0008 derived it from, so a later refresh of the
# measurements does not move it and each rationale names the baseline value
# rather than this file's current one.
#
# Refreshed at `8d7973e` for ADR 0011's reservation-copy removal, two
# consecutive runs on an otherwise idle host (one-minute load average 2.98 and
# 3.44 against this machine's own ~2.9 baseline); the second is recorded and the
# two agreed. The Windows pair was subsequently measured at the same reviewed
# source state, so all four Phase 1 profiles are synchronized. Every budget
# passes. Two measurements moved enough to say so:
#
#   * `load_package_directory` latency rose from ADR 0008's 0.186625 to
#     0.302500, reproduced in both runs, and stays under its retained 0.6
#     ceiling. Nothing in the reviewed source touches directory loading — this
#     branch changes replay capture and the earlier commits changed the archive
#     path — so the standing background load on this named host is the candidate
#     explanation rather than a code regression. Recorded rather than smoothed.
#   * `load_package_archive` peak live heap rose from 193418 to 201987, which is
#     the tracked archive's own 8449 bytes plus its allocation rounding: the
#     accepted cost of reading an archive file through one immutable in-memory
#     copy, decided in ADR 0010.

format_version = 1

[benchmark]
id = "phase-1-deterministic-slice-aarch64-apple-darwin"
workload = "the Phase 1 deterministic replay workflow, eight operations"
phase = "1"
status = "measured"
normative = true
measurements_recorded = true

[profile]
fixture = "fixtures/assets/phase1-slice for matching, fixtures/assets/g-014/valid for loading, mado-pilot-testkit match_fixtures for the scene"
fixture_sha256 = "1defbfec6e34530c0322e8afaf740485a318facc5b864ded8f1b2e8fe4b1af87"
release_target = "aarch64-apple-darwin"
hardware = "Apple M1 Pro, 10 cores, 32 GiB"
os_version = "macOS 26.5.2 build 25F84"
build_profile = "cargo bench, default features, debug_assertions=false"
warmup_iterations = 20
sample_count = 200
correctness_oracle = "every retained sample is checked; each measurement states its own oracle"
queue_policy = "none; every Phase 1 operation is synchronous and no work is queued"
notes = "Apple clang 21.0.0, CMake 4.4.0, rustc 1.97.1, OpenCV 4.14.0. `fixture_sha256` matches the other target's profile for the same benchmark, so both measured the same bytes."

# --- Budgets that apply to every measurement in this file -------------------

[[budget]]
measure = "result_correctness"
kind = "hard"
requirement = "every retained sample satisfies its workload's oracle"
predicate = "result_correctness == 0"

[[budget]]
measure = "allocated_growth_bytes"
kind = "hard"
requirement = "a repeated operation gives back what it took"
# A signed change, so a decrease also satisfies the requirement. Bounded
# rather than exactly zero, per docs/performance.md. One page, because a
# leak of even twenty-four bytes per iteration exceeds it over two hundred
# samples while a single rounded-up allocation does not.
predicate = "allocated_growth_bytes <= 4096"

[[budget]]
measure = "peak_allocated_bytes"
kind = "absolute"
unit = "bytes"
direction = "at_most"
limit = 524288
rationale = "half a mebibyte of live heap for the whole run; the largest measured workload here is 201987 bytes"

# --- Per-workload measurements and budgets ----------------------------------

[[measurement]]
workload = "replay_open"
correctness_oracle = "the session reports the source's own extent and pixel format"
result_correctness = 0
latency_p50_ms = 0.000750
latency_p95_ms = 0.000750
iteration_span_ms = 0.000904
mapped_bytes_per_result = 0
peak_allocated_bytes = 95118
steady_allocated_bytes = 69729
allocated_growth_bytes = 0

  [[measurement.budget]]
  measure = "latency_p95"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 0.0025
  rationale = "three times the 0.000709 measured for ADR 0008's baseline"

[[measurement]]
workload = "map_full_frame"
correctness_oracle = "the mapping covers the whole frame and reports its exact identity"
result_correctness = 0
latency_p50_ms = 0.000041
latency_p95_ms = 0.000042
iteration_span_ms = 0.000076
mapped_bytes_per_result = 24576
peak_allocated_bytes = 69729
steady_allocated_bytes = 69729
allocated_growth_bytes = 0

  # No latency percentile budget. This workload's median is 0.000000 on at
  # least one release target, because the operation is faster than that
  # host's monotonic clock can express. A ceiling on a quantised reading
  # would be a budget on the clock. The rule is written into both targets'
  # files so the two agree about what the workload may do.
  [[measurement.budget]]
  measure = "iteration_span_ms"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 0.0006
  rationale = "three times the 0.000192 measured for ADR 0008's baseline, from one clock reading across the whole sampled run"

  [[measurement.budget]]
  measure = "mapped_bytes_per_result"
  kind = "absolute"
  unit = "bytes"
  direction = "at_most"
  limit = 24576
  rationale = "96x64 at four bytes per pixel, exactly once, into the format the backend requires"

[[measurement]]
workload = "map_region_of_interest"
correctness_oracle = "the mapping covers the requested region and no more"
result_correctness = 0
latency_p50_ms = 0.000250
latency_p95_ms = 0.000250
iteration_span_ms = 0.000308
mapped_bytes_per_result = 6144
peak_allocated_bytes = 75873
steady_allocated_bytes = 69729
allocated_growth_bytes = 0

  [[measurement.budget]]
  measure = "latency_p95"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 0.0008
  rationale = "three times the 0.000250 measured for ADR 0008's baseline"

  [[measurement.budget]]
  measure = "mapped_bytes_per_result"
  kind = "absolute"
  unit = "bytes"
  direction = "at_most"
  limit = 6144
  rationale = "48x32 at four bytes per pixel, which is the ROI the workload maps; a region mapping that mapped the whole frame would be four times this"

[[measurement]]
workload = "load_package_directory"
correctness_oracle = "the package declares its six tracked templates"
result_correctness = 0
latency_p50_ms = 0.264333
latency_p95_ms = 0.302500
iteration_span_ms = 0.272698
mapped_bytes_per_result = 0
peak_allocated_bytes = 146089
steady_allocated_bytes = 69729
allocated_growth_bytes = 0

  [[measurement.budget]]
  measure = "latency_p95"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 0.6
  rationale = "three times the 0.186625 measured for ADR 0008's baseline"

[[measurement]]
workload = "load_package_memory"
correctness_oracle = "the committed package equals the one the same files commit as a directory"
result_correctness = 0
latency_p50_ms = 0.011583
latency_p95_ms = 0.012167
iteration_span_ms = 0.012452
mapped_bytes_per_result = 0
peak_allocated_bytes = 74511
steady_allocated_bytes = 69729
allocated_growth_bytes = 0

  [[measurement.budget]]
  measure = "latency_p95"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 0.04
  rationale = "three times the 0.011250 measured for ADR 0008's baseline"

[[measurement]]
workload = "load_package_archive"
correctness_oracle = "the committed package equals the one the same files commit as a directory"
result_correctness = 0
latency_p50_ms = 0.071291
latency_p95_ms = 0.076334
iteration_span_ms = 0.073198
mapped_bytes_per_result = 0
peak_allocated_bytes = 201987
steady_allocated_bytes = 69729
allocated_growth_bytes = 0

  [[measurement.budget]]
  measure = "latency_p95"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 0.25
  rationale = "three times the 0.072209 measured for ADR 0008's baseline"

[[measurement]]
workload = "prepare_and_match_cold"
correctness_oracle = "the two planted copies are found at their planted offsets, each scoring 1.0 within 1e-5"
result_correctness = 0
latency_p50_ms = 0.261667
latency_p95_ms = 0.279375
iteration_span_ms = 0.462649
mapped_bytes_per_result = 24576
peak_allocated_bytes = 139242
steady_allocated_bytes = 69729
allocated_growth_bytes = 0

  [[measurement.budget]]
  measure = "latency_p95"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 1.0
  rationale = "three times the 0.279708 measured for ADR 0008's baseline"

  [[measurement.budget]]
  measure = "mapped_bytes_per_result"
  kind = "absolute"
  unit = "bytes"
  direction = "at_most"
  limit = 24576
  rationale = "96x64 at four bytes per pixel, exactly once, into the format the backend requires"

[[measurement]]
workload = "match_warm"
correctness_oracle = "the two planted copies are found at their planted offsets, each scoring 1.0 within 1e-5"
result_correctness = 0
latency_p50_ms = 0.254458
latency_p95_ms = 0.272959
iteration_span_ms = 0.257532
mapped_bytes_per_result = 24576
peak_allocated_bytes = 113101
steady_allocated_bytes = 69729
allocated_growth_bytes = 0

  [[measurement.budget]]
  measure = "latency_p95"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 0.8
  rationale = "three times the 0.264583 measured for ADR 0008's baseline"

  [[measurement.budget]]
  measure = "mapped_bytes_per_result"
  kind = "absolute"
  unit = "bytes"
  direction = "at_most"
  limit = 24576
  rationale = "96x64 at four bytes per pixel, exactly once, into the format the backend requires"
