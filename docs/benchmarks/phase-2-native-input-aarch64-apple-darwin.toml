# Phase 2 native input and public-language common flows, measured on
# aarch64-apple-darwin at source dd0f38bc9dc209292ff946f277f442fba52b5d10
# and tree 99b26e9bc5bace43b5dc03e303ecc5c3f0f89f2a.
#
# Produced by `crates/mado-pilot/benches/native-phase2.rs`. ADR 0025 accepts
# this revision-bound replacement for the input profile invalidated by ADR 0021.
# Measurement values are the harness output. Latency ceilings are three times
# measured p95, rounded up; memory and mapped-byte ceilings are stated below.
# These are target- and fixture-specific regression ceilings, not user-facing
# latency promises.

format_version = 1

[benchmark]
id = "phase-2-native-input-aarch64-apple-darwin"
workload = "native input and the public Rust common flow"
phase = "2"
status = "measured"
normative = true
measurements_recorded = true

[profile]
fixture = "crates/platform/macos fixture Rust, protocol, header, and Objective-C sources"
fixture_sha256 = "c8a14353ebd3732ba82895b85b877b5f03f623925ffa0d8b1b9d48383d06fda1"
release_target = "aarch64-apple-darwin"
hardware = "Apple M1 Pro, 10 cores, 32 GiB"
os_version = "macOS 26.5.2 (25F84)"
build_profile = "cargo bench, default features, debug_assertions=false; fixture cargo build --release"
warmup_iterations = 5
sample_count = 50
correctness_oracle = "every retained sample checks frame identity/content, transition state, receipt/event sequence, or complete common-flow outcome as its measurement states"
queue_policy = "session latest-wins queue depth 1; bounded input sequence executes serially"
notes = "source commit dd0f38bc9dc209292ff946f277f442fba52b5d10, tree 99b26e9bc5bace43b5dc03e303ecc5c3f0f89f2a; toolchain rustc 1.97.1 (8bab26f4f 2026-07-14); C/C++ Apple clang 21.0.0; GPU/driver Apple integrated GPU; system driver stack; display topology one built-in 3024x1964 Retina display at scale 2; permissions/signing Screen Recording granted; Accessibility granted; generated fixture bundle ad-hoc signed with approved identifier; fixture path deliberately omitted; C/C++ peak_allocated_bytes counts only harness-side Rust allocations, while peak_resident_bytes is the child process peak reported by the native OS after owned-handle cleanup"

[[budget]]
measure = "result_correctness"
kind = "hard"
requirement = "every retained sample satisfies its workload's oracle"
predicate = "result_correctness == 0"

[[budget]]
measure = "allocated_growth_bytes"
kind = "hard"
requirement = "a repeated operation gives back what it took"
predicate = "allocated_growth_bytes <= 4096"

[[budget]]
measure = "peak_allocated_bytes"
kind = "absolute"
unit = "bytes"
direction = "at_most"
limit = 16777216
rationale = "16 MiB is more than three times the 4635805-byte measured Rust input-request peak"

[[measurement]]
workload = "input_request_receipt"
correctness_oracle = "the complete two-event receipt corresponds to exactly one fixture key-down and key-up summary"
result_correctness = 0
latency_p50_ms = 528.779375
latency_p95_ms = 545.408792
iteration_span_ms = 530.364596
mapped_bytes_per_result = 0
peak_allocated_bytes = 4635805
steady_allocated_bytes = 4635319
allocated_growth_bytes = 0

  [[measurement.budget]]
  measure = "latency_p95"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 1700.0
  rationale = "three times the measured 545.408792 ms, rounded up"

[[measurement]]
workload = "rust_common_flow"
correctness_oracle = "fresh open, frame, mapping, exact fixture input, receipt, and close all complete"
result_correctness = 0
latency_p50_ms = 509.194750
latency_p95_ms = 518.689042
iteration_span_ms = 509.084267
mapped_bytes_per_result = 4628480
peak_allocated_bytes = 4635665
steady_allocated_bytes = 3610
allocated_growth_bytes = 0

  [[measurement.budget]]
  measure = "latency_p95"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 1600.0
  rationale = "three times the measured 518.689042 ms, rounded up"

  [[measurement.budget]]
  measure = "mapped_bytes_per_result"
  kind = "absolute"
  unit = "bytes"
  direction = "at_most"
  limit = 4628480
  rationale = "1280x904 fixture content at four bytes per pixel, exactly once"

[[measurement]]
workload = "c_process_load"
correctness_oracle = "a fresh C process loads and negotiates the released ABI without opening native capabilities"
result_correctness = 0
latency_p50_ms = 231.871583
latency_p95_ms = 233.813625
iteration_span_ms = 231.861554
mapped_bytes_per_result = 0
peak_allocated_bytes = 551
steady_allocated_bytes = 104
allocated_growth_bytes = 0
peak_resident_bytes = 51707904

  [[measurement.budget]]
  measure = "latency_p95"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 750.0
  rationale = "three times the measured 233.813625 ms, rounded up"

  [[measurement.budget]]
  measure = "peak_resident_bytes"
  kind = "absolute"
  unit = "bytes"
  direction = "at_most"
  limit = 201326592
  rationale = "192 MiB is more than three times the measured 51707904-byte child-process high-water mark"

[[measurement]]
workload = "c_common_flow"
correctness_oracle = "the released C ABI performs the same bounded fixture flow in a fresh process"
result_correctness = 0
latency_p50_ms = 1631.164750
latency_p95_ms = 1658.193250
iteration_span_ms = 1762.144290
mapped_bytes_per_result = 4628480
peak_allocated_bytes = 27465
steady_allocated_bytes = 104
allocated_growth_bytes = 0
peak_resident_bytes = 94437376

  [[measurement.budget]]
  measure = "latency_p95"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 5000.0
  rationale = "three times the measured 1658.193250 ms, rounded up"

  [[measurement.budget]]
  measure = "mapped_bytes_per_result"
  kind = "absolute"
  unit = "bytes"
  direction = "at_most"
  limit = 4628480
  rationale = "the released C flow maps one 1280x904 fixture frame exactly once"

  [[measurement.budget]]
  measure = "peak_resident_bytes"
  kind = "absolute"
  unit = "bytes"
  direction = "at_most"
  limit = 301989888
  rationale = "288 MiB is more than three times the measured 94437376-byte child-process high-water mark"

[[measurement]]
workload = "cpp_process_load"
correctness_oracle = "a fresh C++ process loads its wrapper and negotiates the released ABI without opening native capabilities"
result_correctness = 0
latency_p50_ms = 233.757500
latency_p95_ms = 241.062167
iteration_span_ms = 234.673935
mapped_bytes_per_result = 0
peak_allocated_bytes = 563
steady_allocated_bytes = 108
allocated_growth_bytes = 0
peak_resident_bytes = 51757056

  [[measurement.budget]]
  measure = "latency_p95"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 750.0
  rationale = "three times the measured 241.062167 ms, rounded up"

  [[measurement.budget]]
  measure = "peak_resident_bytes"
  kind = "absolute"
  unit = "bytes"
  direction = "at_most"
  limit = 201326592
  rationale = "192 MiB is more than three times the measured 51757056-byte child-process high-water mark"

[[measurement]]
workload = "cpp_common_flow"
correctness_oracle = "the released C++ wrapper performs the same bounded fixture flow in a fresh process"
result_correctness = 0
latency_p50_ms = 1633.213959
latency_p95_ms = 1662.811333
iteration_span_ms = 1768.973633
mapped_bytes_per_result = 4628480
peak_allocated_bytes = 27477
steady_allocated_bytes = 108
allocated_growth_bytes = 0
peak_resident_bytes = 94568448

  [[measurement.budget]]
  measure = "latency_p95"
  kind = "absolute"
  unit = "milliseconds"
  direction = "at_most"
  limit = 5000.0
  rationale = "three times the measured 1662.811333 ms, rounded up"

  [[measurement.budget]]
  measure = "mapped_bytes_per_result"
  kind = "absolute"
  unit = "bytes"
  direction = "at_most"
  limit = 4628480
  rationale = "the released C++ flow maps one 1280x904 fixture frame exactly once"

  [[measurement.budget]]
  measure = "peak_resident_bytes"
  kind = "absolute"
  unit = "bytes"
  direction = "at_most"
  limit = 301989888
  rationale = "288 MiB is more than three times the measured 94568448-byte child-process high-water mark"
